# Copy to .env (git-ignored) for local dev, and set the same values in the
# Netlify dashboard under Site settings → Environment variables for production.

# Required: your Google Gemini API key (server-side only — never exposed to the browser).
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: override the models used by the serverless function.
# GEMINI_TEXT_MODEL=gemini-3.6-flash
# GEMINI_IMAGE_MODEL=gemini-2.5-flash-image

# Optional: skip the vibe image entirely (set to false on the free image tier).
# ENABLE_VIBE_IMAGE=true

# Interim abuse protection on /api/generate (until real auth lands).
# Max generations per client IP per minute (best-effort, per warm instance).
# RATE_LIMIT_PER_MIN=10
# Extra host names allowed to call the endpoint (comma-separated), on top of
# the site's own host + localhost. Usually leave empty.
# ALLOWED_ORIGINS=

# Live traffic map. Public Mapbox token, exposed to the browser at build time —
# restrict it by URL in your Mapbox account settings. Without it, the map shows
# a "not configured" placeholder and the rest of the app works normally.
# VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token
